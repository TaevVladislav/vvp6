#include <iostream>
#include <cmath>
using namespace std;

int main()  //y = 4(x−3)^6 − 7(x−3)^3 + 2 
{
    float x;
    cin >> x;
    float y = 4 * pow(x - 3, 6) - 6 * pow(x - 3, 3) + 2;
    cout << "y = " << y << endl;
}
